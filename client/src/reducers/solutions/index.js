import * as types from '../../actions/constants/actionTypes'

const initialState = {}

export default (state = initialState, action) => {
  switch(action.type){
    case action.type === types.GET_ALL_SOLUTIONS:
      
  }
}